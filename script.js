document.addEventListener("DOMContentLoaded", () => {
    document.getElementById("comment").onclick = (event) => {
        alert("We don't have this feature yet!");
    };
});

// document.addEventListener("DOMContentLoaded", mobile);

// function mobile() {
//     let user = navigator.userAgent;
//     if (user.includes("iPhone") || user.includes("Android")) {
//         document.getElementsByClassName("title")[0].style.maxWidth = "600px";
//     }
// }
